#ifndef CONTROLLER
#define CONTROLLER
#include "raylib.h"

namespace Platformer {
	/// <summary>
	/// Returns char pressed
	/// </summary>
	/// <returns></returns>
	char getKeyPressed();
}

#endif // !CONTROLLER
